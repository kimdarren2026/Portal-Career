# packages/config — Shared Development & Build Configuration

**Status: reserved. Intentionally empty.**

Reserved for shared development and build configuration (linting, formatting, TypeScript or
equivalent compiler settings, test runner configuration) once the toolchain is chosen.

Runtime configuration and secrets do not belong here — see `infra/env/`.
