# Byte-Identical Export Duplicates

Export folders whose contents are byte-identical to another export folder. They were **archived,
not deleted**.

| Original folder | Identical to | Evidence |
| --- | --- | --- |
| `portal_karir_mahasiswa` | `daftar_akun_kandidat_portal_karir_kampus_4` | `code.html` MD5 `b71d4f84…` matches exactly; both titled `Daftar Akun Kandidat - Portal Karir`. Neither folder contains a `screen.png`. |

The retained counterpart is stored at
`archive/stitch-iterations/candidate/daftar-akun-kandidat/iteration-4/`, since the
`daftar-akun-kandidat` screen itself is still pending canonical selection.
