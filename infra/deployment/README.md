# infra/deployment — Deployment Configuration

**Status: reserved and empty.** Deployment manifests and pipeline definitions will be added once a
target environment is chosen (see `docs/decisions/`). No deployment secret may be committed here.
