<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <!-- Isolated Redis database for abuse-control tests; never the runtime
             rate-limit database. -->
        <env name="CACHE_STORE" value="redis"/>
        <env name="REDIS_CACHE_DB" value="15"/>
        <env name="REDIS_PREFIX" value="portal_karir_test_"/>
        <!-- PostgreSQL, never SQLite. The schema depends on partial unique indexes
             and CHECK constraints that SQLite does not share, so an SQLite run
             would silently skip the guarantees that matter most
             (ADR-003, SCHEMA_VALIDATION_REPORT.md §5). -->
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_DATABASE" value="portal_karir_test"/>
        <env name="DB_USERNAME" value="portal_karir_app"/>
        <!-- The test-only migration connection remains distinct from normal
             Laravel runtime traffic. No password is committed. -->
        <env name="DB_MIGRATION_USERNAME" value="portal_karir"/>
        <env name="DB_MIGRATION_PASSWORD" value=""/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
